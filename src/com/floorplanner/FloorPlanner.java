package com.floorplanner;

public class FloorPlanner {
	/*
	 * Store the number of total floors in numTotalFloors variable Store the entered
	 * inputs 1 by 1 into a stack (floor size for each day) After count of entered
	 * numbers equals numTotalFloors -1 sort the floor numbers in descending order,
	 * store their stack positions against it For stack position of (max) until
	 * stack position difference > 1 -> print floors (remove from stack) For stack
	 * position of (next biggest) until stack position difference > 1 -> print
	 * floors (remove from stack) Until stack is empty.
	 * 
	 */

}
